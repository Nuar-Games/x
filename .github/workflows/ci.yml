name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm
      # Exact versions from pnpm-lock.yaml. Fails if package.json and the lockfile disagree.
      - run: pnpm install --frozen-lockfile
      - name: Typecheck
        run: pnpm typecheck
      - name: Tests
        run: pnpm test
      - name: Architecture boundaries
        run: pnpm boundaries
      - name: Determinism
        run: pnpm determinism
      - name: Build
        run: pnpm build
